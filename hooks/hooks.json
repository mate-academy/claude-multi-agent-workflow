{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/scripts/lint-changed.sh\"",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
