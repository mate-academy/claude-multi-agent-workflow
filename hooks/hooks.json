{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | tr -d '\\r' | { read -r f; case \"$f\" in *node_modules*) exit 0;; *.js) npx eslint --fix \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Running eslint --fix"
          }
        ]
      }
    ]
  }
}
