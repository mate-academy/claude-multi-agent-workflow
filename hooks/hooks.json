{
  "hooks": [
    {
      "event": "PostToolUse",
      "match": {
        "tools": ["Edit", "Write"],
        "paths": ["course-api/package.json"]
      },
      "run": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-changelog-sync.js"
    }
  ]
}
