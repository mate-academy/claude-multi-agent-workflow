{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'git commit'; then echo '[dev-pipeline] Commit recorded. Run npm test to verify the full suite still passes.'; fi"
          }
        ]
      }
    ]
  }
}
